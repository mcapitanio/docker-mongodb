version: "3"
services:
  mongodb:
    build:
      context: .
      dockerfile: Dockerfile
    image: mcapitanio/mongodb:3.6
    networks:
    - parrot
    ports:
    - "27017:27017"
networks:
  parrot:
    driver: bridge
